<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <title><%= content_for(:title) || "Task Manager" %></title>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS (with a version update for latest styles) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- Bootstrap Icons (for buttons such as edit & delete) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

    <!-- Font Awesome (for additional icons) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

    <!-- Include Common Stylesheets -->
    <!-- Load Authentication Styles for Login, Sign-up, and Forgot Password -->
    <%= stylesheet_link_tag "authentication", "data-turbo-track": "reload" if params[:controller] == "devise/sessions" || params[:controller] == "devise/registrations" || params[:controller] == "devise/passwords" %>
    <!-- Load Task Styles for Show Page -->
    <%= stylesheet_link_tag "task", "data-turbo-track": "reload" if params[:controller] == "tasks" && params[:action] == "show" %>

    <%= stylesheet_link_tag "styles", "data-turbo-track": "reload" %>

    <!-- Conditionally Load Stylesheet for Task Show Page -->
    <%= stylesheet_link_tag "show", media: "all" if params[:controller] == "tasks" && params[:action] == "show" %>


    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Include Navbar Partial -->
    <%= render "layouts/navbar" %>
    
    <!-- Flash Messages -->
    <div class="container mt-3">
      <% if notice %>
        <div class="alert alert-success">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger">
          <%= alert %>
        </div>
      <% end %>
    </div>
    
    <!-- Main Content -->
    <div class="container mt-4">
      <%= yield %>
    </div>
    
    <!-- Bootstrap JavaScript (for responsive behavior) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
